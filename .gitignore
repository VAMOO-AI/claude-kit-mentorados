# nada de secrets ou estado local
.env
.env.local
*.bak-*
.DS_Store
settings.kit.json

# worktrees locais de agente (estado da máquina, nunca versionado)
.Codex/
.claude/worktrees/
.worktrees/

# worktrees de sessão do Claude Code (git worktree add .claude-worktrees/<nome>)
.claude-worktrees/

# Zip de skill gerado à mão para mandar a alguém (o Finder ainda mete __MACOSX dentro).
# O conteúdo é o mesmo da pasta versionada ao lado — o artefato não entra no git.
plugin/skills/*.zip
