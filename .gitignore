# nada de secrets ou estado local
.env
.env.local
*.bak-*
.DS_Store
settings.kit.json

# worktrees locais de agente (estado da máquina, nunca versionado)
.Codex/
.claude/worktrees/
.worktrees/

# worktrees de sessão do Claude Code (git worktree add .claude-worktrees/<nome>)
.claude-worktrees/
